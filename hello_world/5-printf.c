#include <stdio.h>
/**
 * main - The main entry point
 *
 * Description: Shows the results of printf function
 *
 * Return: Always (0) Succesful
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
