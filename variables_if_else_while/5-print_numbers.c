#include <stdio.h>
/**
 * main - The entry point
 *
 * Description: Print single digit numbers
 *
 * Return: Always 0 success
 */
int main(void)
{
	int num;

	for (num = '0' ; i <= '9' ; num++)
	{
		printf("%d", num);
	}
	printf("\n");
	return (0);
}
